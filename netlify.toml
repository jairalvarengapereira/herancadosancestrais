[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"

# Redirects handled by _redirects

[[plugins]]
  package = "@netlify/plugin-functions-install-core"
